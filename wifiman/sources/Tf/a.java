package Tf;

import Sf.b;
import Sf.c;
import Sf.d;
import Sf.e;
import org.w3c.dom.Document;
import org.w3c.dom.Node;

/* loaded from: classes4.dex */
public interface a {
    Object a(Node node);

    void b(Node node, c cVar, Document document);

    Object c(Node node);

    Object d(Node node);

    void e(Node node, e eVar, Document document);

    void f(Node node, d dVar, Document document);

    Object g(Node node);

    String getId();

    void h(Node node, b bVar, Document document);
}
