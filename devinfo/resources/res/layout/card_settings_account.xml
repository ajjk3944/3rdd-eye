<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android" xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_gravity="center"
    android:id="@+id/account_content"
    android:background="@drawable/bg_common_card_ripple_radius"
    android:padding="@dimen/content_padding"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <com.liuzh.deviceinfo.view.CircleImageView
        android:id="@+id/iv_avatar"
        android:src="@drawable/ic_avatar_not_login"
        android:backgroundTint="@color/avatar_bg_color"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        style="@style/AvatarImage"/>
    <LinearLayout
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:id="@+id/nickname_container"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/content_padding_x1_5"
        app:layout_constraintBottom_toTopOf="@+id/tv_user_info"
        app:layout_constraintEnd_toStartOf="@+id/account_arrow"
        app:layout_constraintStart_toEndOf="@+id/iv_avatar"
        app:layout_constraintTop_toTopOf="@+id/iv_avatar"
        app:layout_constraintVertical_chainStyle="packed">
        <TextView
            android:textSize="16sp"
            android:textColor="?android:attr/textColorPrimary"
            android:id="@+id/tv_nickname"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/login"
            android:singleLine="true"
            android:includeFontPadding="false"
            android:paddingStart="0dp"
            android:paddingEnd="40dp"
            android:textFontWeight="500"/>
        <TextView
            android:id="@+id/tag_user_pro"
            android:visibility="gone"
            android:translationX="-36dp"
            style="@style/ProTag"/>
    </LinearLayout>
    <TextView
        android:textSize="12sp"
        android:id="@+id/tv_user_info"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/content_padding_half"
        android:text="@string/you_have_not_logged_in_yet"
        android:singleLine="true"
        android:includeFontPadding="false"
        android:layout_marginStart="@dimen/content_padding_x1_5"
        app:layout_constraintBottom_toBottomOf="@+id/iv_avatar"
        app:layout_constraintEnd_toStartOf="@+id/account_arrow"
        app:layout_constraintStart_toEndOf="@+id/iv_avatar"
        app:layout_constraintTop_toBottomOf="@+id/nickname_container"/>
    <ImageView
        android:id="@+id/account_arrow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_right_arrow"
        android:contentDescription="@null"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/menu_icon_tint_color"/>
</androidx.constraintlayout.widget.ConstraintLayout>
