import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "@/lib/theme-context";
import { I18nProvider } from "@/lib/i18n";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "ThirdEye - Advanced Network Security",
  description: "Comprehensive wireless security and network analysis tool. Scan WiFi networks, discover Bluetooth devices, monitor network inventory, and test connection speeds.",
  keywords: ["ThirdEye", "Network Security", "WiFi Scanner", "Bluetooth", "Network Analysis", "Security Audit", "Speed Test"],
  authors: [{ name: "ThirdEye Security" }],
  icons: {
    icon: "/3rd_eye.jpeg",
    apple: "/3rd_eye.jpeg",
  },
  openGraph: {
    title: "ThirdEye",
    description: "Advanced network security and analysis tool",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
        suppressHydrationWarning
      >
        <ThemeProvider>
          <I18nProvider>
            {children}
            <Toaster />
          </I18nProvider>
        </ThemeProvider>
      </body>
    </html>
  );
}
