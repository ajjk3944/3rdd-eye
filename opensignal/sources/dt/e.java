package dt;

import java.util.ArrayList;
import java.util.List;
import ls.g0;
import ls.q0;
import ls.v0;
import ls.y0;

/* loaded from: classes.dex */
public interface e {
    List c(w wVar, rs.b bVar, a aVar);

    ArrayList f(v0 v0Var, ns.f fVar);

    ArrayList h(q0 q0Var, ns.f fVar);

    List j(w wVar, g0 g0Var);

    ArrayList n(u uVar);

    List o(w wVar, g0 g0Var);

    List p(w wVar, ls.t tVar);

    List q(w wVar, rs.b bVar, a aVar, int i10, y0 y0Var);

    List t(w wVar, rs.b bVar, a aVar);
}
